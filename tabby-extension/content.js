// Content script - included as a helper if needed
(() => {
  // No-op for now; background uses executeScript to collect text when needed.
  // This file can be used to add observers for dynamic pages later.
})();
